Cipher is a web-based steganography tool for hiding and extracting messages in images (PNG), audio (WAV) and mp4. It uses LSB, parity emebed algorithm with AES, SHA-256, scramble algorithm . Users also can download outputs. 
Tech Stack: HTML, CSS, JavaScript, Java (Spring Boot), MySQL 
